\documentclass[12pt,a4paper]{article}
\usepackage[margin=2.5cm]{geometry}
\usepackage{graphicx} % Required for inserting images
\usepackage{float}
\usepackage{tikz}
\usetikzlibrary{decorations.pathreplacing,calc}
\usepackage{subcaption}
\usepackage{booktabs}
\usepackage{parskip}
\usepackage{gensymb}
\usepackage{biblatex}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{cleveref}
\addbibresource{ref.bib}
\title{Clock Recovery}
\author{Joe Leacy}

\begin{document}

\maketitle

\chapter{Clock Recovery}
\section{Introduction}
\section{Godard Timing Recovery}
\paragraph{Timing error estimate}
A common timing error detector (TED) is the Gardner algorithm which computes an error term from samples $x[n]$ as \cite{Zhou;10}
\begin{equation}
    e[k] = \Re\left\{x^*[2k-1](x[2k]-x[2k-2])\right\}
\end{equation}
which is then fed through a digital phase-locked loop (DPLL) and interpolator to correct timing errors. However, this requites 2 samples per symbol which is prohibitive for modern coherent receivers which operate at low oversampling rates to reduce complexity. The signal can be upsampled inside the clock recovery block to apply Gardner but a more energy-efficient approach is to use the Godard TED \cite{Godard;78}. An RRC pulse has a bandwidth of $\frac{1+\beta}{T}$ where $\beta$ is the rolloff factor and $T$ is the symbol period, so for the Fourier transform of the input signal $X(f)$, $C(f) = X(f)X^*(f-\frac{1}{T})$ is non-zero where the spectra overlap. It is also real-valued in this region due to the symmetry of the RRC spectrum. A timing offset $\tau$ introduces a phase ramp in  the frequency domain so the overlap becomes
\begin{equation}
    C(f) = X(f)X^*(f-\frac{1}{T})e^{-j2\pi f\tau}e^{j2\pi (f-1/T)\tau} = X(f)X^*(f-\frac{1}{T})e^{-2\pi\tau / T}.
\end{equation}
Thus the timing offset $\tau$ can be recovered from the phase of $C(f)$, which is independent of $f$. A discrete equivalent is given by \cite{Han;11}
\begin{equation}
    C[k] = X[k]X^*[k+N/2].
\end{equation}
The independence of the phase of $C[k]$ on $k$ allows a more robust estimate of $\tau$ to be calculated from
\begin{equation}
    S = \sum_{k=0}^{N/2-1}C[k].
\end{equation}
The standard Godard TED also appears to require 2 samples per symbol, but a modified version for arbitrary oversampling rates $\eta$ is given by \cite{Josten;17}
\begin{equation}
    S = \sum_{k=\frac{1-\beta}{2\eta}N}^{\frac{1+\beta}{2\eta}N} X[k]X^*[k+(1 - 1/\eta)N].
\end{equation}
The timing error estimate normalised to the sampling period is then
\begin{equation}
    \hat{\tau}_s = \frac{\eta\hat{\tau}}{T} = -\frac{\eta}{2\pi}\arg(S).
    \label{eq:godard_est}
\end{equation}

\end{document}
